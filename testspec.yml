version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
  pre_build:
    commands:
      - echo Installing source NPM dependencies on production...
      - npm install
  build:
    commands:
      - echo Testing...
      - CI=true npm run test

artifacts:
  files:
    - '**/*'