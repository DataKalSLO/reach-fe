version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - echo Testing...
      - CI=true npm run test

artifacts:
  files:
    - '**/*'

cache:
  paths:
    - './node_modules/**/*'